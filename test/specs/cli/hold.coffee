describe "hold", ->

  it "should hold dependencies from node_modules", ->
  it "should hold only specified package if it's name passed", ->

  context "if version not specified", ->

    it "should fetch version from npmjs.org unless it's present in node_modules", ->
    it "should take version from node_modules if module already installed", ->